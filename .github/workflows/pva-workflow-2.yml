name: pva-workflow-2
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "GitHub environment name to get the solution"
        required: true
        default: "DEV"
env:
  workflow_scripts_path: .github/workflows/scripts

jobs:
  execute-pva-workflow-1:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - uses: actions/checkout@v2
      - name: check-branch-existence
        id: check-branch-existence
        uses: actions/github-script@v5
        with:
          github-token: ${{secrets.WORKFLOW_DISPATCH_TOKEN}}
          script: |
            let branch = await github.rest.git.listMatchingRefs({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'heads/${{ secrets.BRANCH_TO_CREATE }}'
            })

            console.log(branch)
            /*
            if (branch.data.message != 'Not Found')
            {
              await github.rest.git.deleteRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: 'heads/${{ secrets.BRANCH_TO_CREATE }}'
              })
            }
            */

      - name: check-branch-existence-2
        id: check-branch-existence-2
        if: ${{ failure() }}
        uses: actions/github-script@v5
        with:
          github-token: ${{secrets.WORKFLOW_DISPATCH_TOKEN}}
          script: |
            console.log('job failed')

      - name: check-branch-existence-3
        id: check-branch-existence-3
        if: ${{ success() }}
        uses: actions/github-script@v5
        with:
          github-token: ${{secrets.WORKFLOW_DISPATCH_TOKEN}}
          script: |
            console.log('job succeed')